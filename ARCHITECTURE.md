# System Architecture - Personal Cloud Storage

## 🏗️ Overall Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     CLIENT (Browser)                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Frontend (HTML5, CSS3, JavaScript)                      │   │
│  │  ┌────────────────┐  ┌────────────────┐                 │   │
│  │  │ Auth Pages     │  │ Dashboard      │                 │   │
│  │  │ - Register     │  │ - File List    │                 │   │
│  │  │ - Login        │  │ - Upload       │                 │   │
│  │  │ - Logout       │  │ - Download     │                 │   │
│  │  │                │  │ - Delete       │                 │   │
│  │  │                │  │ - Search       │                 │   │
│  │  │                │  │ - Filter       │                 │   │
│  │  │                │  │ - Sort         │                 │   │
│  │  │                │  │ - Dark Mode    │                 │   │
│  │  └────────────────┘  └────────────────┘                 │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  JWT Token: localStorage                                       │
│  API Calls: Fetch API with Authorization headers              │
└─────────────────────────────────────────────────────────────────┘
                            │
                  ┌─────────┴─────────┐
                  │  HTTPS/HTTP       │
                  │  Port 5000        │
                  │  CORS Enabled     │
                  │
┌─────────────────────────────────────────────────────────────────┐
│               SERVER (Node.js + Express)                        │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                   Express App                            │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │   │
│  │  │ Auth Routes  │  │ File Routes  │  │   Middleware │   │   │
│  │  │              │  │              │  │              │   │   │
│  │  │ POST /       │  │ GET /        │  │ JWT Auth     │   │   │
│  │  │  register    │  │  files       │  │ Multer       │   │   │
│  │  │              │  │              │  │ Upload       │   │   │
│  │  │ POST /login  │  │ POST /upload │  │ CORS         │   │   │
│  │  │              │  │              │  │              │   │   │
│  │  │              │  │ GET /        │  │              │   │   │
│  │  │              │  │  download/:id│  │              │   │   │
│  │  │              │  │              │  │              │   │   │
│  │  │              │  │ DELETE /:id  │  │              │   │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘   │   │
│  │                                                          │   │
│  │  ┌──────────────┐  ┌──────────────┐                    │   │
│  │  │ Controllers  │  │ Utils        │                    │   │
│  │  │              │  │              │                    │   │
│  │  │ auth         │  │ Bcrypt       │                    │   │
│  │  │ file         │  │ JWT          │                    │   │
│  │  └──────────────┘  └──────────────┘                    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │           Static File Server                             │   │
│  │  /uploads/  → Serve files to frontend                   │   │
│  │  /client/   → Serve HTML, CSS, JS                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                            │
                  ┌─────────┴─────────┐
                  │  MongoDB Driver   │
                  │  Mongoose ODM     │
                  │
┌─────────────────────────────────────────────────────────────────┐
│                    MongoDB Database                             │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  Collections:                                            │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │ Users Collection                                   │ │   │
│  │  │  ├─ _id                                            │ │   │
│  │  │  ├─ username (unique)                              │ │   │
│  │  │  ├─ email (unique)                                 │ │   │
│  │  │  ├─ password (hashed)                              │ │   │
│  │  │  └─ createdAt                                      │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  │                                                          │   │
│  │  ┌────────────────────────────────────────────────────┐ │   │
│  │  │ Files Collection                                   │ │   │
│  │  │  ├─ _id                                            │ │   │
│  │  │  ├─ originalName                                   │ │   │
│  │  │  ├─ storedName (unique)                            │ │   │
│  │  │  ├─ filePath                                       │ │   │
│  │  │  ├─ fileType (images, videos, documents, others)   │ │   │
│  │  │  ├─ fileSize                                       │ │   │
│  │  │  ├─ userId (references Users)                      │ │   │
│  │  │  └─ uploadDate                                     │ │   │
│  │  └────────────────────────────────────────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                            │
                  ┌─────────┴─────────┐
                  │  File System      │
                  │
┌─────────────────────────────────────────────────────────────────┐
│              File Storage on Disk                               │
│  /server/uploads/                                              │
│  ├── images/                                                   │
│  │   └── {userId}/                                             │
│  │       ├── 1712345678_abcd1234_photo.jpg                    │
│  │       └── 1712345679_efgh5678_image.png                    │
│  ├── videos/                                                   │
│  │   └── {userId}/                                             │
│  │       └── 1712345680_ijkl9012_movie.mp4                    │
│  ├── documents/                                                │
│  │   └── {userId}/                                             │
│  │       ├── 1712345681_mnop3456_report.pdf                   │
│  │       └── 1712345682_qrst7890_sheet.xlsx                   │
│  └── others/                                                   │
│      └── {userId}/                                             │
│          └── 1712345683_uvwx1234_file.zip                     │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### 1. Registration Flow

```
┌──────────┐
│ Browser  │
└────┬─────┘
     │ Fill form (username, email, password)
     │ Click Register
     ▼
┌──────────────────────────────────────┐
│ Register POST /api/auth/register     │
├──────────────────────────────────────┤
│ 1. Validate input                    │
│    - Username: letters/numbers only  │
│    - Email: @gmail.com format        │
│    - Password: 6+ characters         │
│                                      │
│ 2. Check if user exists              │
│    - Query: User.findOne({email})    │
│    - Query: User.findOne({username}) │
│                                      │
│ 3. Hash password (bcrypt)            │
│                                      │
│ 4. Create new User                   │
│    - Save to MongoDB                 │
│                                      │
│ 5. Generate JWT token                │
│    - Payload: {id, username}         │
│    - Expiry: 7 days                  │
│                                      │
│ 6. Return token to client            │
└──────────┬───────────────────────────┘
           │ {token, user}
           ▼
┌──────────────────────┐
│ Browser              │
│ - Store token        │
│ - localStorage.set   │
│ - Redirect dashboard │
└──────────────────────┘
```

### 2. Login Flow

```
┌──────────┐
│ Browser  │
└────┬─────┘
     │ Fill form (email, password)
     │ Click Login
     ▼
┌──────────────────────────────┐
│ Login POST /api/auth/login   │
├──────────────────────────────┤
│ 1. Find user by email        │
│    - Query: User.findOne()   │
│                              │
│ 2. Compare password          │
│    - bcrypt.compare()        │
│                              │
│ 3. If match:                 │
│    Generate JWT token        │
│    Return token              │
│                              │
│ 4. If no match:              │
│    Return error              │
└──────────┬────────────────────┘
           │ {token, user}
           ▼
┌──────────────────────┐
│ Browser              │
│ - Store token        │
│ - localStorage.set   │
│ - Redirect dashboard │
└──────────────────────┘
```

### 3. File Upload Flow

```
┌──────────┐
│ Browser  │
└────┬─────┘
     │ Select file(s)
     │ Click Upload
     │ Send FormData with file
     ▼
┌──────────────────────────────────┐
│ Upload POST /api/files/upload    │
├──────────────────────────────────┤
│ 1. Auth middleware               │
│    - Extract Bearer token        │
│    - Verify JWT signature        │
│    - Get user ID                 │
│                                  │
│ 2. Multer (File Processing)      │
│    - Detect MIME type            │
│    - Categorize file type        │
│    - Generate unique filename:   │
│      {timestamp}_{random}_{orig} │
│    - Create user directory:      │
│      /uploads/{type}/{userId}/   │
│    - Save file to disk           │
│                                  │
│ 3. Save metadata to MongoDB      │
│    - Create File document        │
│    - Store: name, type, size,    │
│      path, userId, uploadDate    │
│                                  │
│ 4. Return metadata to client     │
└──────────┬──────────────────────┘
           │ {file, message}
           ▼
┌──────────────────────┐
│ Browser              │
│ - Show success       │
│ - Update file list   │
│ - Display file item  │
└──────────────────────┘
```

### 4. File Download Flow

```
┌──────────┐
│ Browser  │
└────┬─────┘
     │ Click Download
     │ Send file ID
     ▼
┌──────────────────────────────────────┐
│ Download GET /api/files/download/:id │
├──────────────────────────────────────┤
│ 1. Auth middleware                   │
│    - Verify token                    │
│    - Get user ID                     │
│                                      │
│ 2. Find file in MongoDB              │
│    - Query: File.findById(id)        │
│                                      │
│ 3. Verify ownership                  │
│    - Check: file.userId == req.user  │
│                                      │
│ 4. Read file from disk               │
│    - Get file path                   │
│    - Read binary data                │
│                                      │
│ 5. Send file to client               │
│    - Set headers: Content-Type,      │
│      Content-Disposition             │
│    - Stream file data                │
└──────────┬───────────────────────────┘
           │ File binary + headers
           ▼
┌──────────────────────┐
│ Browser              │
│ - Download started   │
│ - Save to computer   │
└──────────────────────┘
```

### 5. File Delete Flow

```
┌──────────┐
│ Browser  │
└────┬─────┘
     │ Click Delete
     │ Confirm deletion
     │ Send file ID
     ▼
┌──────────────────────────────────┐
│ Delete DELETE /api/files/:id     │
├──────────────────────────────────┤
│ 1. Auth middleware               │
│    - Verify token                │
│    - Get user ID                 │
│                                  │
│ 2. Find file in MongoDB          │
│    - Query: File.findById(id)    │
│                                  │
│ 3. Verify ownership              │
│    - Check: file.userId == user  │
│                                  │
│ 4. Delete from filesystem        │
│    - Get file path               │
│    - fs.unlinkSync()             │
│                                  │
│ 5. Delete from MongoDB           │
│    - File.findByIdAndDelete()    │
│                                  │
│ 6. Return success                │
└──────────┬──────────────────────┘
           │ {message}
           ▼
┌──────────────────────┐
│ Browser              │
│ - Show success       │
│ - Remove from list   │
│ - Reload files       │
└──────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────┐
│           Request with Token                        │
│                                                     │
│  Authorization: Bearer eyJhbGciOiJIUzI1NiIs...     │
└────────────────┬──────────────────────────────────┘
                 │
                 ▼
        ┌────────────────────┐
        │ Auth Middleware    │
        └────────┬───────────┘
                 │
        ┌────────┴──────────┐
        │                   │
    Has Token?          No Token?
        │                   │
        ▼                   ▼
   ┌─────────┐         ┌──────────┐
   │ Verify  │         │ Return   │
   │ Signature│        │ 401      │
   └────┬────┘         │ Error    │
        │              └──────────┘
    ┌───┴────────┐
    │            │
Valid?      Invalid?
    │            │
    ▼            ▼
 ┌────┐    ┌──────────┐
 │Add  │    │ Return   │
 │user │    │ 401      │
 │to   │    │ Error    │
 │req  │    └──────────┘
 └────┘
    │
    ▼
Continue to Route Handler
```

---

## 📁 Directory Structure

```
personal-cloud-storage/
│
├── client/                      # Frontend application
│   ├── index.html              # Main HTML
│   ├── css/
│   │   └── styles.css          # Styling
│   └── js/
│       ├── api.js              # API client functions
│       ├── auth.js             # Auth logic
│       └── dashboard.js        # Dashboard logic
│
├── server/                      # Backend application
│   ├── config/
│   │   └── database.js         # MongoDB configuration
│   │
│   ├── controllers/            # Business logic
│   │   ├── authController.js   # Register/Login
│   │   └── fileController.js   # File operations
│   │
│   ├── middleware/             # Express middleware
│   │   ├── auth.js             # JWT verification
│   │   └── upload.js           # File upload handling
│   │
│   ├── models/                 # Database schemas
│   │   ├── User.js             # User schema
│   │   └── File.js             # File schema
│   │
│   ├── routes/                 # API routes
│   │   ├── auth.js             # Auth endpoints
│   │   └── files.js            # File endpoints
│   │
│   ├── uploads/                # File storage
│   │   ├── images/
│   │   ├── videos/
│   │   ├── documents/
│   │   └── others/
│   │
│   ├── server.js               # Express app
│   ├── package.json            # Dependencies
│   ├── .env                    # Configuration
│   └── .env.example            # Config template
│
├── Documentation/
│   ├── README.md               # Project overview
│   ├── SETUP_GUIDE.md          # Installation guide
│   ├── API_DOCUMENTATION.md    # API reference
│   ├── CONFIGURATION_CHECKLIST.md
│   └── IMPLEMENTATION_COMPLETE.md
│
├── Scripts/
│   ├── START_SERVER.bat        # Windows startup
│   └── start_server.sh         # Mac/Linux startup
│
└── ARCHITECTURE.md (this file)
```

---

## 🔄 Request/Response Cycle

```
CLIENT REQUEST
     │
     ├─→ Fetch API
     │   ├─→ URL: http://IP:5000/api/...
     │   ├─→ Method: GET/POST/DELETE
     │   ├─→ Headers: Authorization, Content-Type
     │   └─→ Body: JSON data
     │
     ▼
SERVER RECEIVES
     │
     ├─→ Express Router
     ├─→ Middleware Chain
     │   ├─→ CORS Handler
     │   ├─→ JSON Parser
     │   ├─→ Auth Middleware (if needed)
     │   └─→ Multer (if file upload)
     │
     ├─→ Route Handler
     │   ├─→ Controller Logic
     │   ├─→ Database Operations
     │   └─→ File System Operations
     │
     ▼
SERVER RESPONSE
     │
     ├─→ JSON Response
     ├─→ HTTP Status Code
     └─→ Headers
     │
     ▼
CLIENT RECEIVES
     │
     ├─→ Parse JSON
     ├─→ Update UI
     ├─→ Show Notification
     └─→ Handle Errors
```

---

## 🚀 Network Communication

```
┌─────────────────────────────────────────────────┐
│            CLIENT DEVICE                        │
│  (Any device on network: PC, Phone, Tablet)     │
│                                                 │
│  Browser: http://192.168.1.100:5000            │
│  (automatically resolves to server IP)          │
└────────────────┬────────────────────────────────┘
                 │
      ┌──────────┴──────────┐
      │   Local Network     │
      │   (WiFi/Ethernet)   │
      └──────────┬──────────┘
                 │
┌────────────────┴──────────────────────────────┐
│         SERVER DEVICE                         │
│  Express on 0.0.0.0:5000                      │
│                                               │
│  ├─→ Receives requests from any device        │
│  ├─→ Processes file operations                │
│  └─→ Sends responses back                     │
│                                               │
│  /server/uploads/ → File storage              │
│  MongoDB ← Database operations                │
└───────────────────────────────────────────────┘
```

---

This architecture ensures:

- ✅ Secure authentication
- ✅ User file isolation
- ✅ Conflict prevention
- ✅ Network accessibility
- ✅ Scalable design
- ✅ Clear separation of concerns
